Forecast evaluation is performed with strict timestamp alignment using a shared validity mask. For each horizon h, targets are defined as `series.shift(-h)`, and both persistence-baseline and model predictions are filtered on the same valid index before error computation. This protocol guarantees that all methods are compared on identical timestamps at each horizon, reducing the risk of horizon-dependent sampling differences and preventing evaluation bias.
